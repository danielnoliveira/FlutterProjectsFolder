import 'package:flutter/material.dart';

class CountryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detalhamento de País'),
      ),
      body: Column(
        children: [Text('Tela de detalhamento do pais')],
      ),
    );
  }
}
